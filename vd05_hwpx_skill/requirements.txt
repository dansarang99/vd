# ==============================================================================
# vd05_hwpx_skill 의존성 패키지 (requirements.txt)
# HWPX (OWPML) 생성, 무결성 검증, 자동 줄맞춤 및 레이아웃 캐시 제거
# ==============================================================================
#
# 대부분의 핵심 엔진(hwpx_engine, psst_builder, table_builder, verify_hwpx)은
# Python 표준 라이브러리(zipfile, xml.sax, xml.etree, re, pathlib 등)만을 사용합니다.
# autofit_table_rows.py의 고급 OWPML 태그 핸들링을 위해 lxml이 사용됩니다.

lxml>=4.9.0
